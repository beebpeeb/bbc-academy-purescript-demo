{
  "name": "bbc-academy-puresript-demo",
  "private": true,
  "source": "assets/index.html",
  "scripts": {
    "build": "spago build",
    "generateDocs": "spago docs --open",
    "release": "spago build && parcel build",
    "repl": "spago repl",
    "start": "spago build && parcel --open",
    "test": "spago test"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "parcel": "^2.6.2",
    "purescript": "^0.15.3",
    "spago": "^0.20.9"
  }
}
